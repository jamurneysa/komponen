class Env {
  // static const String baseUrl = (kIsWeb) ? 'localhost:8080' : 'api.winwin.web.id';
  static const String baseUrl = 'api.winwin.web.id';
  static const String wsUrl = 'wss://api.winwin.web.id';
}

var prosestxt = '';
